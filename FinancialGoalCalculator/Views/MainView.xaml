<UserControl x:Class="Finance.UserControls.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#E67E22">
            <DockPanel LastChildFill="True">

                <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Left"
                    Margin="15,0">
                    <Image Source="/Assets/logo.png"
                   Width="24"
                   Height="24"/>
                    <TextBlock Text="Финансовые цели"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Right"
                    Margin="10,0">
                    <Button Content="Создать цель"
                    Click="btnCreateGoal_Click"
                    Style="{StaticResource LightOrangeButton}"
                    Margin="0,12,10,12"/>

                    <Button Content="Выйти"
                    Click="btnExit_Click"
                    Style="{StaticResource DeleteButton}"/>
                </StackPanel>


                <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                    <Button Content="Все цели"
                    Tag="0"
                    Click="btnGoals_Click"
                    Style="{StaticResource SidebarButton}"/>

                    <Button Content="Покупки"
                    Tag="1"
                    Click="btnGoals_Click"
                    Style="{StaticResource SidebarButton}"/>

                    <Button Content="Сбережения"
                    Tag="2"
                    Click="btnGoals_Click"
                    Style="{StaticResource SidebarButton}"/>

                    <Button Content="Инвестиции"
                    Tag="3"
                    Click="btnGoals_Click"
                    Style="{StaticResource SidebarButton}"/>
                </StackPanel>

            </DockPanel>
        </Border>

        <Frame Grid.Row="1"
           x:Name="MainContentFrame"
           NavigationUIVisibility="Hidden"/>
    </Grid>


</UserControl>
